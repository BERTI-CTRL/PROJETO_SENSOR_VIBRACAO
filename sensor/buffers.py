#buffers.py
from collections import deque

buffer_ax = deque(maxlen=10000)
buffer_ay = deque(maxlen=10000)
buffer_az = deque(maxlen=10000)

buffer_gx = deque(maxlen=10000)
buffer_gy = deque(maxlen=10000)
buffer_gz = deque(maxlen=10000)